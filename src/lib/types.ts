export type toolsData = {
    Program:string;
    Command:string;
    portFlag:string;
}

export type progressFlags = {
    allBruteSSH:boolean;
    allFTPCrack:boolean;
    allRelaySMTP:boolean;
    allHTTPWorm:boolean;
    allSQLInject:boolean;
}